# העלאה לנטליפיי

## לפני הכל - המפתח שנשרף

המפתח שהודבק בצ'אט חשוף. ב-`console.anthropic.com` -> API Keys: **מחק אותו, צור חדש.**
`.env` המקומי לא עולה לגיט (מוגן ב-`.gitignore`). בפרודקשן המפתח נכנס דרך ממשק נטליפיי בלבד.

---

## 1. גיט

הפרויקט עדיין לא ריפו. מהשורש:

```bash
git init
git add -A
git status          # ודא ש-.env ו-node_modules לא ברשימה
git commit -m "feat: כלי ניתוח דירה להשקעה עם שמשון"
```

אם `.env` מופיע ב-`git status` - **עצור.** הוא אמור להיות מסונן.

צור ריפו ריק ב-GitHub ואז:

```bash
git remote add origin <כתובת-הריפו>
git branch -M main
git push -u origin main
```

---

## 2. נטליפיי

1. `app.netlify.com` -> Add new site -> Import an existing project -> GitHub -> בחר את הריפו
2. הגדרות הבילד נקראות מ-`netlify.toml` אוטומטית. אל תשנה ידנית:
   - Build command: `npm run data:boi && npm run build`
   - Publish directory: `dist`
3. **לפני הדיפלוי הראשון** - Site configuration -> Environment variables -> Add:

   | Key | Value |
   |---|---|
   | `ANTHROPIC_API_KEY` | המפתח **החדש** |

4. Deploy.

---

## 2.5 ביקורת דיפלוי - מה נבדק ומה תוקן

בוצעה ביקורת לפני העלייה. הממצאים:

| נבדק | תוצאה |
|---|---|
| מפתח דולף ל-`dist/` | **נקי.** אפס מופעים של `sk-ant` |
| `.env` נכנס לגיט | **מסונן.** בטוח |
| הפונקציה בתיקייה הנכונה | כן, `netlify/functions/shimshon.mts` |
| תלויות הפונקציה | `@anthropic-ai/sdk@0.127.0` ו-`@netlify/blobs@11.1.0` מותקנות ומיובאות |
| סדר ה-redirects | `/api/shimshon` לפני `/*`. תקין |
| הפונקציה מקצה לקצה | `npm run smoke:shimshon` עובר |

**באג שתוקן בביקורת:** `getStore()` של נטליפיי זורק **בקריאה עצמה** כשהסביבה
לא מוגדרת, וה-`try/catch` עטף רק את הקריאה שאחריה. בפרודקשן זה היה מפיל את
שמשון ב-500. עכשיו הוא ממשיך לעבוד בלי מכסה במקום לקרוס.

**נקודת כשל שתוקנה בפקודת הבילד:** היה `npm run data:boi && npm run build`.
הסקריפט יוצא בקוד 1 כשה-API של בנק ישראל נופל, ולכן **תקלה זמנית אצלם הייתה
מפילה את כל הדיפלוי.** עכשיו הוא ממשיך עם ה-snapshot המחויב ומדפיס אזהרה.
נבדק בסימולציה.

**Netlify Blobs:** נדרש למכסת השאלות. הוא זמין אוטומטית לפונקציות v2 בנטליפיי.
אם משום מה לא יהיה - שמשון ימשיך לעבוד, פשוט בלי תקרה יומית. שווה לוודא
אחרי הדיפלוי הראשון (ראה טבלת הבדיקות למטה).

---

## 3. רענון יומי של נתוני בנק ישראל

הבילד מושך נתונים טריים, אבל בילד קורה רק בדחיפה לגיט. בלי טריגר יומי
הנתונים קופאים על מועד הדחיפה האחרונה.

**ה-workflow כבר כתוב:** `.github/workflows/refresh-data.yml`. רץ כל יום ב-03:00 UTC
ודופק על Build Hook של נטליפיי. נשארו לך שני צעדים ידניים:

1. **בנטליפיי:** Site configuration -> Build & deploy -> Build hooks -> Add build hook.
   תן לו שם כמו `daily-refresh`. **העתק את ה-URL.**
2. **בגיטהאב:** Settings -> Secrets and variables -> Actions -> New repository secret.
   שם: `NETLIFY_BUILD_HOOK`, ערך: ה-URL מהשלב הקודם.

**לבדוק שזה עובד בלי לחכות ליום:** לשונית Actions -> "רענון נתוני בנק ישראל" ->
Run workflow. אם הסוד חסר, ה-workflow נכשל בהודעה ברורה במקום בשקט.

---

## 4. בדיקה אחרי העלייה

| מה | איך | תקין אם |
|---|---|---|
| האתר עולה | פתח את הכתובת | המסך נטען, מספרים מוצגים |
| נתוני בנק ישראל | כרטיס הריבית | מציג ריבית ותאריך |
| שמשון | לחץ "שאל את שמשון" -> "למה התזרים שלי שלילי?" | תשובה מוזרמת תוך שניות |
| המכסה | 11 שאלות ברצף | ה-11 נחסמת בהודעה בעברית |
| המפתח לא דלף | DevTools -> Sources, חפש `sk-ant` | **אפס תוצאות** |

---

## מה עוד לא נבנה

| | סטטוס |
|---|---|
| טסטי המלכודת של שמשון | נכתבו, **לא רצו** - חסר מפתח ב-`.env` |
| רענון יומי (cron) | ה-workflow כתוב. נדרשים 2 צעדים ידניים - ראה סעיף 3 |
| גרפים | `d3-scale`/`d3-shape` מותקנים, לא בשימוש |
| שמירת נכסים | `idb` מותקן, לא בשימוש |
| דוח הדפסה | לא נבנה |
| מחירי עסקאות מ-govmap | נחקר ואומת, לא מומש |
| מדדי למ"ס | נחקרו ואומתו, לא מומשו |

---

## נתונים שעדיין לא אומתו - לפני שיווק אמיתי

| נתון | ערך | בעיה |
|---|---|---|
| תקרת פטור ממס שכ"ד | 5,654 ₪ | של 2025, לא 2026 |
| אגרות טאבו | 600 / 125 ₪ | המקור בלוג משפטי, לא משרד המשפטים |
| טווחי עלות בעלי מקצוע | ראה `docs/research/professional-cost-ranges.md` | רוב המקורות אתרי שיווק של נותני שירות |
