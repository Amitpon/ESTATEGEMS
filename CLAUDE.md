# CLAUDE.md - כלי ניתוח דירות להשקעה

> מסמך הפרויקט. כל סוכן קורא אותו לפני שהוא נוגע בקוד.
> כפוף ל-CLAUDE.md הגלובלי של המשתמש. מסמך זה מוסיף עליו, לא סותר אותו.

---

## מה זה המוצר

כלי web לניתוח כדאיות של דירה להשקעה **בישראל**. המשתמש מזין נתוני עסקה ומקבל תזרים, תשואה, מיסוי וניתוח משכנתא. שומר נכסים ומשווה ביניהם.

**קהל:** משקיע פרטי ישראלי, לא איש פיננסים.

**ברירת המחדל היא דירה ראשונה** (הכרעת בעל המוצר, 2026-09-21). המשתמש מסמן במפורש
אם זו אינה הדירה הראשונה שלו. ההבחנה הזו משנה **שני** דברים מהותיים:
מדרגות מס הרכישה, והזכאות לפטור ממס שבח במכירה (סעיף 49ב(2)).
לכן מס שבח הוא פיצ'ר מרכזי, לא שולי.

**רקע:** קיים מוצר קודם - [EstateGems](https://estate-gems.base44.app) שנבנה ב-base44 על ידי בעל המוצר. הפרויקט הזה הוא שכתוב שלו. מודל הנתונים והמדדים שלו **נכונים ומוכחים - להיצמד אליהם**. מה שנשבר שם: RTL, מובייל, טיפוגרפיה. ראה `docs/research/estategems-teardown.md`.

---

## עקרונות מוצר מחייבים

**`docs/product-principles.md` גובר על כל המלצה במסמכי המחקר.** תמצית:

1. **הכלי לא מנבא.** כל מספר עתידי צמוד להנחה שיצרה אותו. אין ניסוח של ודאות ("שווי הנכס יהיה") ואין ניסוח של המלצה ("נכס מומלץ"). מספר עתידי מעוגל גס.
2. **נתונים חיצוניים הם עוגן, לא מילוי אוטומטי.** מציגים למשתמש נתון השוואה עם מקור ותאריך. הוא מזין בעצמו.
3. **שמשון (עוזר ה-AI) מסביר ומכוון, לא לוקח אחריות.** לא ממציא מספר, לא מנבא, לא ממליץ על החלטה. כל מספר שהוא מזכיר מגיע מהמנוע או מהדאטה. ראה עיקרון 4.
4. **שלוש שכבות עומק:** התשובה -> הפירוק -> ההסבר. ברירת המחדל רדודה. **אין הנחה נסתרת בקוד** - כל קבוע חישובי הוא שדה גלוי וניתן לעריכה.

---

## החלטות ארכיטקטורה

| נושא | הכרעה | למה |
|---|---|---|
| ריצה | אתר סטטי. `npm run dev` מקומית = אותו קוד שעולה לנטליפיי | אין DB ואין auth. **חריג יחיד:** Netlify Function ל-proxy של שמשון, ראה למטה |
| פרטיות | כל נתוני המשתמש ב-IndexedDB במכשיר שלו בלבד | שום דבר לא עוזב את הדפדפן, **פרט לשאילתה יזומה לשמשון** (עיקרון 4) |
| היקף | ישראל בלבד, שקלים בלבד | אין בורר מדינה ואין בורר מטבע. חו"ל נדחה |
| דאטה חיצוני | build-time snapshot, לא fetch חי | ה-API של בנק ישראל לא מחזיר CORS headers. רענון יומי דרך Scheduled Function |
| מובייל | Mobile-first הוא הדיפולט, לא fallback | מתכננים ל-390px ואז מרחיבים |

---

## Stack

React 19 + TypeScript + Vite 8 + Tailwind 4 + shadcn/ui. אותה משפחה כמו המוצר הקיים, עם TypeScript.

**החלטות מ-`docs/research/dependency-audit.md` - אל תבטל בלי לקרוא אותו:**

| במקום | משתמשים | למה |
|---|---|---|
| recharts | `d3-scale` + `d3-shape` + SVG ידני | ~100kb, וכואבת ב-RTL של הצירים |
| jspdf + html2canvas | `window.print()` + print stylesheet | ~200kb, **ו-html2canvas שבורה ברינדור עברית** |
| react-router-dom | `wouter` | 20kb מול 2kb, ל-4 מסכים |
| date-fns / numeral | `Intl` נייטיבי | אפס kb. `Intl.NumberFormat('he-IL')` |
| tailwindcss-rtl | logical properties של Tailwind 4 | החבילה נטושה מ-2022 |
| maplibre-gl | `d3-geo` + SVG (אם בכלל) | 220kb |

יעד משקל: **~174kb gzip**, טעינה ראשונית ~120-130kb.

**לא להתקין חבילה חדשה בלי לשאול.** זה כלל גלובלי של המשתמש.

---

## בעלות קבצים

שני סוכנים לעולם לא עורכים את אותו קובץ. לפני שאתה כותב - ודא שאתה בתחום שלך.

| תחום | בעלים | מכיל |
|---|---|---|
| `src/lib/calc/`, `src/types/` | calc-engine | מנוע החישוב. פונקציות טהורות בלבד |
| `src/services/`, `scripts/`, `src/data/` | boi-integrator | שכבת נתונים חיצוניים |
| `src/components/`, `src/styles/` | ui-designer | design system וקומפוננטות |
| `src/components/ui/Disclosure.tsx` | ui-designer | סקשן מתקפל. **מבוסס `<details>` בכוונה** - כך הוא נפתח אוטומטית בהדפסה ובחיפוש בעמוד. אל תמיר ל-state |
| `src/pages/` | feature implementors | מסכים |
| `netlify/functions/` | boi-integrator | Scheduled Function לרענון, ו-proxy של שמשון |
| `docs/research/` | סוכני מחקר | מסמכי מחקר |
| `docs/design/` | ui-designer | אפיון עיצובי |
| `CLAUDE.md`, `docs/PROGRESS.md`, `docs/product-principles.md` | PM | תיאום ועקרונות |

---

## כללי קוד

- **`strict: true`, `noUncheckedIndexedAccess: true`.** אין `any`.
- **מנוע החישוב הוא פונקציות טהורות.** בלי React, בלי DOM, בלי fetch, בלי `Date.now()` בפנים - תאריכים מגיעים כפרמטר.
- **כל פונקציית חישוב מחזירה גם את הפירוק**, לא רק את הסכום. שכבה 2 של הממשק תלויה בזה.
- **כל מספר רגולטורי** (מדרגת מס, תקרה) יושב בקובץ נתונים עם `asOf`, `source`, ו-`verified`. לא מפוזר בקוד.
- העדף פשוט על חכם. אבסטרקציה רק אחרי 3 שימושים.
- הערות מסבירות **למה**, לא **מה**.
- **לפני שקוד ממשק מחשב משהו מ-`combinedRows` - בדוק אם המנוע כבר חושף שדה מוכן.** נמצאה כפילות חוזרת: `ResultsPage.tsx`, `PrintReport.tsx:79` ו-`ReportInsights.tsx:69` כולם חישבו מחדש `totalInterest` עם `combinedRows.reduce(...)` במקום לקרוא ל-`result.data.mortgage.totalInterest` שהמנוע כבר מחזיר (`src/lib/calc/mortgage.ts`, `MortgageResult.totalInterest`). שלושתם עדיין לא תוקנו - ראה `docs/ROADMAP.md`.

## שיטת הסילוקין

המנוע תומך ב-`spitzer` וב-`equalPrincipal` דרך `financing.tracks[].amortization`.
שתיהן נתמכו מההתחלה. אם משהו בממשק "לא תומך" בשיטה - הבעיה בממשק, לא במנוע.

## טבלת המכירה - שורות מפתח

ההרצה מייצרת שורות שנתיות. `withExemptionPoint` מזריק שורה נוספת בתאריך
שבו הפטור ממס שבח נכנס (18 חודשים מהאכלוס), כי הוא כמעט אף פעם לא נופל
על שנה עגולה. **בלי השורה הזו התשואה השנתית מחולקת במספר שגוי של שנים.**

שורה כזו נושאת `label` ו-`saleDate` מפורש. קוד שסופר שורות בטבלה חייב
לסנן לפי `label` אם הוא מצפה לשנים עגולות בלבד.

## RTL

- `dir="rtl"` על ה-`<html>`. זה קיים ב-`index.html` ואסור להסיר.
- **logical properties בלבד:** `ms-` / `me-` / `ps-` / `pe-` / `start-` / `end-`. אף פעם לא `ml-` / `mr-` / `left-` / `right-`.
- **מספרים הם LTR בתוך טקסט RTL.** `1,850,000 ₪` נקרא משמאל לימין.
- `tabular-nums` על כל מספר בטבלה או ברשימה, אחרת העמודות רוקדות.
- **ציר X בגרפים נשאר LTR** (זמן הוא קונבנציה אוניברסלית). ציר Y עובר לימין.
- **הפרדה בין עמודות בגריד סטטיסטי (N עמודות עם קווי הפרדה):** `border-e` (logical - מתהפך נכון ב-RTL) יחד עם ריפוד אסימטרי `pe-*`/`ps-*`. **לא** `gap-*` - זה מכפיל את הרווח מול הריפוד שכבר קיים סביב הבורדר. מאומת ב-`ResultsPage.tsx`, גריד המדדים הראשיים.

## טקסט למשתמש

- **מקף רגיל בלבד. לעולם לא em-dash.** חל על כל תווית, כפתור, טקסט עזר, tooltip והודעת שגיאה.
- עברית ברורה. הודעת שגיאה אומרת מה לעשות, לא רק מה נכשל.
- הטון לפי `docs/design/design-spec.md`.

## טסטים

- `vitest`. מנוע החישוב חייב **כיסוי מלא** - זה הלב, ואם המספרים שגויים הכלי חסר ערך.
- טסט חייב לאמת ערך, לא רק שהפונקציה לא זרקה.
- מקרי קצה חובה: אפס הון עצמי, הון עצמי מלא, שכ"ד 0, תקופה של שנה.
- `npm test` עובר לפני שמשהו נחשב "done".
- **תווית טקסט שעלולה להופיע פעמיים במסך (למשל גם כתווית מדד וגם ככותרת עמודה בטבלה) - סקוף את ה-selector.** `getByText('כיסוי ההחזר')` נשבר כי אותה תווית מופיעה גם ב-`<div>` בגריד המדדים החדש וגם ב-`<th>` ב-`RateSensitivityTable` שמוצג באותו עמוד. תיקון: `{ selector: 'div' }`. אל תניח ייחודיות של טקסט - בדוק עם `getAllByText` או סקופ מפורש.

---

## פקודות

```bash
npm run dev        # פיתוח מקומי
npm run build      # build סטטי לנטליפיי
npm test           # טסטים
npm run data:boi   # משיכת נתוני ריבית מבנק ישראל ל-snapshot
```

---

## נתונים לא מאומתים - לטפל לפני עלייה לאוויר

| נתון | ערך | סטטוס |
|---|---|---|
| תקרת פטור ממס שכ"ד | 5,654 ₪ | **[?] של 2025.** לאמת מול רשות המסים |
| ריבית בנק ישראל | 3.25% | נמשך מה-API ב-2026-09-20, מתעדכן דרך `npm run data:boi` |
| מדרגות מס רכישה | 8% עד 6,055,070 ₪ | קפואות עד ינואר 2028 |
| מונחים במילון של שמשון | 53 מונחים | **[!] Haiku טועה בביטחון במונחים ישראליים.** נתפס בבדיקה טוען שטופס 4 הוא מסמך של המוכר לרשם המקרקעין. המילון בהוראות הקבועות מכסה 53 מונחים; מונח מחוצה לו עדיין בסיכון |
| אגרת רישום משכנתא בטאבו | 600 ₪ | **[?] לאמת מול justice.gov.il.** המקור שנמצא הוא בלוג משפטי |
| אגרת רישום הערת אזהרה | 125 ₪ | **[?] לאמת מול justice.gov.il** |
| תקרת פטור ממס שבח | 5,008,000 ₪ | **[!] מיושן.** המקור הרשמי אומר שזה תקף ל-2024 בלבד. התקרה מתעדכנת שנתית. דרוש הוראת ביצוע 2026 |
| יתר נתוני מס שבח | ראה `docs/research/capital-gains-tax.md` | **[?] לא אומתו.** לא להציג למשתמש בלי תווית |
| טווחי עלות בעלי מקצוע | ראה `docs/research/professional-cost-ranges.md` | **רובם מאתרי שיווק של נותני שירות.** אינדיקציה בלבד, מוצג כטווח ולא כברירת מחדל |

כל נתון כזה מוצג למשתמש **עם תאריך ומקור**.

---

## הדפסה ודוח

`window.print()` **לבדו לא מספיק.** המסך בנוי מ-`<details>` מקופלים,
והדפדפן מדפיס רק את מה שפתוח. השתמש ב-`printReport()` מ-`src/lib/print.ts`.

**`print:open` אינו קיים.** `open` היא תכונת HTML ולא CSS, ו-Tailwind
פשוט לא מייצר כלל. class לא מוכר **נעלם בשקט** ו-`tsc` לא מתלונן -
אחרי variant חריג ודא ב-`dist/assets/*.css` שנוצר CSS.

השחזור אחרי הדפסה תלוי ב-`afterprint` ולא סינכרוני. בספארי ובמובייל
`print()` חוזר מיד, ושחזור סינכרוני סוגר את הסקשנים לפני הצילום.

## החלטות פתוחות

ראה `docs/ROADMAP.md` להמשך העבודה ולרעיונות, ו-`docs/PROGRESS.md` להיסטוריה. אל תכריע לבד בנושא מוצרי - שאל את בעל המוצר.
</content>
